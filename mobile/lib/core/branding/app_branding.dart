/// Centralized brand identity — the single source of truth for the product
/// name so it never gets hardcoded (and drifts) across screens.
abstract final class AppBranding {
  static const String appName = 'UZDONATE';
  static const String legalName = 'UzDonate';
}
